---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: teamhealthrequests.assist.cluster.local
spec:
  group: assist.cluster.local
  names:
    kind: TeamHealthRequest
    listKind: TeamHealthRequestList
    plural: teamhealthrequests
    singular: teamhealthrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Current phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Brief summary
      jsonPath: .status.summary
      name: Summary
      type: string
    - description: Namespaces checked
      jsonPath: .status.namespacesChecked
      name: Namespaces
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TeamHealthRequest is the Schema for the teamhealthrequests API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TeamHealthRequestSpec defines the desired state of TeamHealthRequest
            properties:
              checks:
                description: 'checks specifies which checkers to run (default: all
                  available)'
                items:
                  description: CheckerName identifies a specific checker
                  enum:
                  - workloads
                  - helmreleases
                  - kustomizations
                  - gitrepositories
                  - secrets
                  - pvcs
                  - quotas
                  - networkpolicies
                  type: string
                type: array
              config:
                description: config contains checker-specific configuration
                properties:
                  pvcs:
                    description: pvcs configuration
                    properties:
                      capacityWarningPercent:
                        default: 85
                        description: capacityWarningPercent is the percentage of PVC
                          capacity that triggers a warning
                        format: int32
                        maximum: 100
                        minimum: 1
                        type: integer
                    type: object
                  quotas:
                    description: quotas configuration
                    properties:
                      usageWarningPercent:
                        default: 80
                        description: usageWarningPercent is the percentage of quota
                          usage that triggers a warning
                        format: int32
                        maximum: 100
                        minimum: 1
                        type: integer
                    type: object
                  secrets:
                    description: secrets configuration
                    properties:
                      certExpiryWarningDays:
                        default: 30
                        description: certExpiryWarningDays is the number of days before
                          expiry to warn
                        format: int32
                        minimum: 1
                        type: integer
                      checkCertExpiry:
                        default: true
                        description: checkCertExpiry enables TLS certificate expiry
                          checking
                        type: boolean
                      checkReferences:
                        default: false
                        description: checkReferences verifies secrets are referenced
                          by workloads
                        type: boolean
                    type: object
                  workloads:
                    description: workloads configuration
                    properties:
                      includeJobs:
                        default: false
                        description: includeJobs includes Job and CronJob resources
                        type: boolean
                      restartThreshold:
                        default: 3
                        description: restartThreshold is the number of restarts that
                          triggers a warning
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                type: object
              notify:
                description: notify configures notification targets for health check
                  results
                items:
                  description: NotificationTarget defines where to send notifications
                  properties:
                    onCompletion:
                      default: true
                      description: onCompletion sends notification when health check
                        completes
                      type: boolean
                    onSeverity:
                      description: onSeverity sends notification only when issues
                        of this severity or higher are found
                      enum:
                      - Critical
                      - Warning
                      - Info
                      type: string
                    secretRef:
                      description: secretRef references a Secret containing the webhook
                        URL
                      properties:
                        key:
                          description: key is the key within the Secret
                          type: string
                        name:
                          description: name is the name of the Secret
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    type:
                      description: type is the notification backend type
                      enum:
                      - webhook
                      - slack
                      - pagerduty
                      - teams
                      type: string
                    url:
                      description: url is the webhook URL (use this OR secretRef,
                        not both)
                      type: string
                  required:
                  - type
                  type: object
                type: array
              scope:
                description: scope defines which namespaces to check
                properties:
                  currentNamespaceOnly:
                    default: false
                    description: currentNamespaceOnly limits checks to the namespace
                      where the request is created
                    type: boolean
                  namespaceSelector:
                    description: namespaceSelector selects namespaces by labels
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  namespaces:
                    description: namespaces is an explicit list of namespaces to check
                    items:
                      type: string
                    type: array
                type: object
              ttlSecondsAfterFinished:
                description: |-
                  ttlSecondsAfterFinished limits the lifetime of a completed/failed request.
                  After this duration, the request is automatically deleted.
                  If unset, the request is never auto-deleted.
                format: int32
                minimum: 0
                type: integer
            type: object
          status:
            description: TeamHealthRequestStatus defines the observed state of TeamHealthRequest
            properties:
              completedAt:
                description: completedAt is when the health check completed or failed
                format: date-time
                type: string
              conditions:
                description: conditions represent the current state of the TeamHealthRequest
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastCheckTime:
                description: lastCheckTime is when the health check was last performed
                format: date-time
                type: string
              namespacesChecked:
                description: namespacesChecked lists the namespaces that were checked
                items:
                  type: string
                type: array
              phase:
                description: phase indicates the current phase of the health check
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              results:
                additionalProperties:
                  description: CheckerResult contains results from a single checker
                  properties:
                    error:
                      description: error is set if the checker failed to run
                      type: string
                    healthy:
                      description: healthy is the count of healthy resources
                      format: int32
                      type: integer
                    issues:
                      description: issues is the list of problems found
                      items:
                        description: DiagnosticIssue represents a discovered problem
                        properties:
                          message:
                            description: message is a human-readable description of
                              the issue
                            type: string
                          severity:
                            description: severity indicates how serious the issue
                              is
                            enum:
                            - Critical
                            - Warning
                            - Info
                            type: string
                          suggestion:
                            description: suggestion provides actionable advice to
                              fix the issue
                            type: string
                          type:
                            description: type categorizes the issue
                            type: string
                        required:
                        - message
                        - severity
                        - type
                        type: object
                      type: array
                  required:
                  - healthy
                  type: object
                description: results contains results grouped by checker
                type: object
              summary:
                description: summary provides a brief overview of findings
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
